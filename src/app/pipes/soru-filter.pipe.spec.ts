import { SoruFilterPipe } from './soru-filter.pipe';

describe('SoruFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SoruFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
